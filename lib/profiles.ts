import type { Athlete, Trainer } from '@prisma/client'
export function athleteCompletion(profile: Partial<Athlete>) { const fields = [profile.firstName, profile.lastName, profile.primarySport, profile.positions?.length, profile.graduationYear, profile.city, profile.bio, profile.school, profile.gpa, profile.hudlUrl || profile.highlightVideoUrl, profile.achievements]; return Math.round(fields.filter(Boolean).length / fields.length * 100) }
export function publicAthlete(profile: Athlete) { return { slug: profile.slug, firstName: profile.firstName, lastName: profile.lastName, profilePhotoUrl: profile.profilePhotoUrl, primarySport: profile.primarySport, positions: profile.positions, jerseyNumber: profile.jerseyNumber, height: profile.height, graduationYear: profile.graduationYear, city: profile.city, state: profile.state, bio: profile.bio, school: profile.school, gpa: profile.gpa, hudlUrl: profile.hudlUrl, highlightVideoUrl: profile.highlightVideoUrl, socialLinks: profile.socialLinks, achievements: profile.achievements, trainingExperience: profile.trainingExperience } }
export function publicTrainer(profile: Trainer) { return { slug: profile.slug, displayName: profile.displayName, profilePhotoUrl: profile.profilePhotoUrl, sports: profile.sports, specialties: profile.specialties, yearsExperience: profile.yearsExperience, playingExperience: profile.playingExperience, coachingExperience: profile.coachingExperience, bio: profile.bio, city: profile.city, state: profile.state, country: profile.country, latitude: profile.latitude, longitude: profile.longitude, verified: profile.verified, featured: profile.featured } }
export function slugify(value: string) { return value.toLowerCase().trim().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '') }
