'use client'

import { useState } from 'react'

type Profile = { firstName: string; lastName: string; sport: string; position: string; graduation: string; city: string; state: string; height: string; gpa: string; school: string; bio: string; film: string; achievements: string[] }
const steps = ['About You', 'Your Game', 'Academics', 'Experience', 'Preview', 'Publish']
const base: Profile = { firstName: 'Avery', lastName: 'Mitchell', sport: 'Basketball', position: 'Point Guard', graduation: '2027', city: 'Los Angeles', state: 'CA', height: "5'9\"", gpa: '3.8', school: 'Westlake High School', bio: 'A high-motor guard who creates pace, makes reads, and brings energy to every possession.', film: '', achievements: ['All-League First Team', '2025 Summer Showcase Invite'] }
const Field = ({ label, value, onChange, placeholder }: { label: string; value: string; onChange: (value: string) => void; placeholder?: string }) => <label className="pc-field"><span>{label}</span><input value={value} placeholder={placeholder} onChange={(event) => onChange(event.target.value)} /></label>

export default function PlayerCardBuilder() {
 const [step, setStep] = useState(0); const [profile, setProfile] = useState(base); const [saved, setSaved] = useState(false); const [published, setPublished] = useState(false)
 const set = (key: keyof Profile) => (value: string) => setProfile({ ...profile, [key]: value })
 const next = () => setStep(Math.min(step + 1, 5)); const previous = () => setStep(Math.max(step - 1, 0)); const completion = Math.min(100, 52 + Math.round(Object.values(profile).filter(Boolean).length * 6))
 const preview = <div className="pc-profile"><div className="pc-profile-top"><span>thePORTAL ATHLETE</span><span>PLAYER CARD</span></div><div className="pc-avatar">{profile.firstName[0]}{profile.lastName[0]}</div><h2>{profile.firstName} {profile.lastName}</h2><p className="pc-role">{profile.position} · {profile.sport} · {profile.graduation}</p><div className="pc-stats"><span><b>{profile.height || '—'}</b>HEIGHT</span><span><b>{profile.gpa || '—'}</b>GPA</span><span><b>{profile.city || '—'}</b>LOCATION</span></div><p>{profile.bio || 'Tell your athlete story.'}</p><div className="pc-tags">{profile.achievements.map((item) => <span key={item}>{item}</span>)}</div></div>
 return <main className="player-card-page"><header className="trainers-nav"><a className="brand" href="/">the<span>PORTAL</span></a><nav aria-label="Main navigation"><a href="/trainers">Preferred Trainers</a><a className="nav-cta" href="/player-card">Build your Player Card <span aria-hidden="true">↗</span></a></nav></header>
  <section className="pc-hero"><div><p className="eyebrow">Your free athlete identity</p><h1>Build your<br /><em>Player Card.</em></h1><p>Your game deserves more than a résumé. Bring your athletic identity, academics, achievements, training, and story into one professional profile.</p><div className="hero-actions"><button className="button button-primary" onClick={() => document.getElementById('builder')?.scrollIntoView({ behavior: 'smooth' })}>Build my free Player Card <span>↗</span></button><a className="text-link" href="#example">See an example <span>↓</span></a></div><p className="pc-free">$0 — Always free. No credit card required.</p></div><div id="example">{preview}</div></section>
  <section id="builder" className="pc-builder"><div className="pc-builder-heading"><div><p className="eyebrow">Your Player Card</p><h2>Make your story<br /><em>discoverable.</em></h2></div><div className="pc-save"><b>{completion}%</b> Player Card complete <button onClick={() => setSaved(true)}>Save progress</button>{saved && <small>Saved locally</small>}</div></div><div className="pc-steps">{steps.map((item, index) => <button key={item} onClick={() => setStep(index)} className={index === step ? 'active' : index < step ? 'done' : ''}><span>{index + 1}</span>{item}</button>)}</div><div className="pc-workspace"><section className="pc-form"><p className="eyebrow">Step {step + 1} of 6</p><h2>{steps[step]}</h2>{step === 0 && <div className="pc-fields"><Field label="First name" value={profile.firstName} onChange={set('firstName')} /><Field label="Last name" value={profile.lastName} onChange={set('lastName')} /><Field label="Sport" value={profile.sport} onChange={set('sport')} /><Field label="Primary position" value={profile.position} onChange={set('position')} /><Field label="Graduation year" value={profile.graduation} onChange={set('graduation')} /><Field label="City" value={profile.city} onChange={set('city')} /><Field label="State" value={profile.state} onChange={set('state')} /><Field label="Height" value={profile.height} onChange={set('height')} /></div>}{step === 1 && <div className="pc-fields"><Field label="Primary sport" value={profile.sport} onChange={set('sport')} /><Field label="Position" value={profile.position} onChange={set('position')} /><label className="pc-field full"><span>Short athlete bio</span><textarea value={profile.bio} onChange={(event) => set('bio')(event.target.value)} /></label><Field label="Hudl / film URL" value={profile.film} onChange={set('film')} placeholder="https://" /></div>}{step === 2 && <div className="pc-fields"><Field label="GPA (self-reported)" value={profile.gpa} onChange={set('gpa')} /><Field label="High school" value={profile.school} onChange={set('school')} /><Field label="Intended major" value="" onChange={() => {}} placeholder="Optional" /><p className="pc-note">Academic information is self-reported until verified through thePORTAL.</p></div>}{step === 3 && <div><p className="pc-copy">Add the teams, training, honors, and defining moments that show how you compete.</p>{profile.achievements.map((item, index) => <Field key={index} label={`Achievement ${index + 1}`} value={item} onChange={(value) => { const achievements = [...profile.achievements]; achievements[index] = value; setProfile({ ...profile, achievements }) }} />)}<button className="pc-add" onClick={() => setProfile({ ...profile, achievements: [...profile.achievements, ''] })}>+ Add achievement</button><button className="pc-add">+ Add experience</button></div>}{step === 4 && <div>{preview}<button className="pc-add" onClick={() => setStep(0)}>Edit card</button></div>}{step === 5 && <div className="pc-live"><span>✓</span><h3>Your Player Card is live.</h3><p>Share your professional athlete identity with coaches, trainers, schools, teams, and mentors.</p><code>/athletes/avery-mitchell</code><div><button className="button button-primary" onClick={() => setPublished(true)}>Copy link</button><a className="button button-outline" href="/athletes/avery-mitchell">Preview card</a></div>{published && <small>Profile link copied</small>}</div>}<div className="pc-actions">{step > 0 && <button className="button button-outline" onClick={previous}>Back</button>}{step < 5 && <button className="button button-primary" onClick={next}>{step === 4 ? 'Publish my Player Card' : 'Continue'} <span>→</span></button>}</div></section><aside className="pc-side"><p className="eyebrow">Always included</p><h3>Own your profile.</h3><ul><li>Professional digital athlete profile</li><li>Athletic and academic information</li><li>Achievements, film, and social links</li><li>Shareable Player Card</li><li>Updates anytime, always free</li></ul><div className="pc-powered"><p className="eyebrow">Your next level</p><b>Powered-On Profile</b><p>Verification can unlock aligned thePORTAL opportunities. Creating a Player Card does not mean verification, recruitment, or guaranteed opportunities.</p><a href="#powered">Learn about Powered-On →</a></div></aside></div></section>
  <section id="powered" className="pc-closing"><p className="eyebrow">Be ready when opportunity finds you</p><h2>Your next opportunity starts with being <em>discoverable.</em></h2><p>Build your free Player Card and put everything that makes you an athlete in one place.</p><button className="button button-primary" onClick={() => document.getElementById('builder')?.scrollIntoView({ behavior: 'smooth' })}>Build my free Player Card <span>↗</span></button></section>
 </main>
}
